const colors = {
  black: (alpha: number = 1) => `rgba(0, 0, 0, ${alpha})`, // #000,
  white: (alpha: number = 1) => `rgba(255, 255, 255, ${alpha})`, // #fff,
  epaBlue: '#0a71b9',
};

export { colors };
